<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp"
    app:strokeColor="@color/card_stroke"
    app:strokeWidth="1dp"
    app:cardBackgroundColor="@color/white"
    android:layout_margin="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Gambar produk - kotak penuh atas -->
        <ImageView
            android:id="@+id/ivProductIcon"
            android:layout_width="match_parent"
            android:layout_height="90dp"
            android:src="@drawable/ic_food_placeholder"
            android:background="@drawable/bg_icon_green_light"
            android:scaleType="centerInside"
            android:padding="16dp"/>

        <!-- Info bawah -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="8dp"
            android:paddingTop="6dp"
            android:paddingBottom="8dp">

            <TextView
                android:id="@+id/tvNamaProduk"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Produk"
                android:textSize="12sp"
                android:fontFamily="@font/poppins_semibold"
                android:textColor="@color/text_primary"
                android:maxLines="2"
                android:ellipsize="end"
                android:layout_marginBottom="2dp"/>

            <!-- Stok di kiri, kontrol jumlah (minus/jumlah/plus) di kanan -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <TextView
                    android:id="@+id/tvStok"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Stok: 0"
                    android:textSize="11sp"
                    android:fontFamily="@font/poppins_regular"
                    android:textColor="@color/text_secondary"/>

                <!-- Tombol - (hanya muncul jika jumlah > 0) -->
                <ImageView
                    android:id="@+id/btnKurang"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:src="@drawable/ic_remove"
                    android:background="@drawable/bg_btn_kurang"
                    android:tint="@color/green_primary"
                    android:padding="5dp"
                    android:layout_marginEnd="6dp"
                    android:visibility="gone"/>

                <!-- Jumlah produk di cart (hanya muncul jika jumlah > 0) -->
                <TextView
                    android:id="@+id/tvJumlahDiCart"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="0"
                    android:minWidth="18dp"
                    android:gravity="center"
                    android:textSize="12sp"
                    android:fontFamily="@font/poppins_semibold"
                    android:textColor="@color/text_primary"
                    android:layout_marginEnd="6dp"
                    android:visibility="gone"/>

                <!-- Tombol + -->
                <ImageView
                    android:id="@+id/btnTambah"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:src="@drawable/ic_add"
                    android:background="@drawable/bg_btn_add"
                    android:tint="@color/white"
                    android:padding="6dp"/>
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- Overlay untuk disable state -->
    <View
        android:id="@+id/overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#80FFFFFF"
        android:visibility="gone"/>

</com.google.android.material.card.MaterialCardView>
